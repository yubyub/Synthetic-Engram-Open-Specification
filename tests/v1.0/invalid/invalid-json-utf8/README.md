# invalid-json-utf8

Rejects a JSON document that is not UTF-8. `engram.json.hex` is the exact byte
recipe materialized as `engram.json` in a temporary test directory; storing the
recipe as text keeps repository tooling and patches UTF-8 safe.
